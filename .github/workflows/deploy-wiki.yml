name: Deploy Wiki
run-name: Updating the wiki
on:
  push:
    branches:
      - main
jobs:
  send-request:
    runs-on: ubuntu-latest
    steps:
      run: curl -X GET hhr-wiki.cheljessen.net:6402/github
